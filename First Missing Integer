#This file takes a list of elements whose values are in range [1,N+1], where N is size of list
#It returns the first missing positive integer.
#For this to succeed, Input must be unique, and in the range [1,N+1] only

def missInt(l,n):
    sum = 0
    k = int((n+1)*(n+2)/2)    #sum of first N+1 elements = k
    for i in l:
        sum += i              #Sum of elements of list = sum
    return (k - sum)          #this difference gives the missing integer from the list
                              #provided that input constraints are followed.


#Driver code to test the function missInt()
n = int(input("Enter range:"))
l = []
print("Enter elements:")
for i in range(n):
    l.append(int(input()))
print(l)
print("The missing number is",missInt(l,n))
 
