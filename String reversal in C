#include <stdio.h>
#include <string.h>
int main()
{
    char s[100],temp;
    int i,j;
    scanf("%s",s);
    int l = strlen(s);
    for(i=0,j=l-1;i<j;i++,j--) 
    {
        temp = s[i];
        s[i] = s[j];    
        s[j] = temp;
    }
    printf("%s",s);
    return 0;
}
